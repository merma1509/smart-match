services:
  smart-match:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - ./uploads:/app/uploads
    environment:
      - KMP_DUPLICATE_LIB_OK=TRUE
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G
